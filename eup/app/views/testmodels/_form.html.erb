<%= form_for(@testmodel) do |f| %>
  <% if @testmodel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testmodel.errors.count, "error") %> prohibited this testmodel from being saved:</h2>

      <ul>
      <% @testmodel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
